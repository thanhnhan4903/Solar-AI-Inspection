Created At: 2026-06-05T13:35:50Z
Completed At: 2026-06-05T13:35:51Z
The following changes were made by the replace_file_content tool to: c:\Solar_Inspection_Project\datn_be\scratch\edge_grid_full_image_DJI_0029.py. If relevant, proactively run terminal commands to execute this code for the USER. Don't ask for permission.
[diff_block_start]
@@ -389,69 +389,42 @@
             
     return gap_lines
 
-def evaluate_and_snap_oriented_line(gray, y_anchor, x_mid, m_perp, x_start, x_end, n_samples=20, max_offset=3):
-    """
-    Evaluates and snaps an oriented horizontal line: y = m_perp * (x - x_mid) + y_anchor
-    Using a 1D projection profile check within a tight normal search range of ±3 px.
-    """
-    xs = np.linspace(x_start, x_end, n_samples)
-    offsets = []
-    support_points = []
-    
-    # 1. Build a local 1D profile of intensity across normal offsets [-3, 3]
-    profile_sums = np.zeros(2 * max_offset + 1)
-    profile_counts = np.zeros(2 * max_offset + 1)
-    
-    for dy in range(-max_offset, max_offset + 1):
-        for x in xs:
-            y_est = m_perp * (x - x_mid) + y_anchor
-            x_idx = int(round(x))
-            py = int(round(y_est + dy))
-            py_clamped = max(0, min(gray.shape[0] - 1, py))
-            profile_sums[dy + max_offset] += gray[py_clamped, x_idx]
-            profile_counts[dy + max_offset] += 1
-            
-    profile = profile_sums / np.maximum(profile_counts, 1)
-    smoothed_profile = np.convolve(profile, np.ones(3)/3.0, mode="same")
-    
-    valley_idx = np.argmin(smoothed_profile)
-    offset = valley_idx - max_offset
-    
-    y_snapped_anchor = y_anchor + offset
-    
-    # Calculate support points and support ratio
-    for x in xs:
-        y_est = m_perp * (x - x_mid) + y_snapped_anchor
-        x_idx = int(round(x))
-        support_points.append((x_idx, y_est))
-        
-    # Count how many individual sample points have their local valley close to the snapped offset
-    sup_cnt = 0
-    for x in xs:
-        # Local valley search at 
<truncated 948 bytes>
t <= 2.5 and contrast_ok
-    return y_snapped_anchor, m_perp, support_ratio, mean_abs_offset, max_abs_offset, 0.0, support_points, is_good
+def evaluate_angle_candidate(gray, raw_gaps, x_mid, theta, n_samples=25):
+    m = math.tan(theta)
+    cos_theta = math.cos(theta)
+    sin_theta = math.sin(theta)
+    total_depth = 0.0
+    
+    max_offset = 6
+    for y_anchor, x_start, x_end in raw_gaps:
+        xs = np.linspace(x_start, x_end, n_samples)
+        profile_sums = np.zeros(2 * max_offset + 1)
+        profile_counts = np.zeros(2 * max_offset + 1)
+        
+        for dn in range(-max_offset, max_offset + 1):
+            for x in xs:
+                x_sample = x - dn * sin_theta
+                y_sample = m * (x - x_mid) + y_anchor + dn * cos_theta
+                x_idx = int(round(x_sample))
+                y_idx = int(round(y_sample))
+                if 0 <= x_idx < gray.shape[1] and 0 <= y_idx < gray.shape[0]:
+                    profile_sums[dn + max_offset] += gray[y_idx, x_idx]
+                    profile_counts[dn + max_offset] += 1
+                    
+        profile = profile_sums / np.maximum(profile_counts, 1)
+        smoothed = np.convolve(profile, np.ones(3)/3.0, mode="same")
+        
+        best_depth = 0.0
+        for v in range(1, len(smoothed) - 1):
+            if smoothed[v] < smoothed[v-1] and smoothed[v] < smoothed[v+1]:
+                left_peak = np.max(smoothed[:v])
+                right_peak = np.max(smoothed[v+1:])
+                depth = min(left_peak, right_peak) - smoothed[v]
+                if depth > best_depth:
+                    best_depth = depth
+        total_depth += best_depth
+    return total_depth
+
 
 def get_oriented_line_y_at_x(m, x_mid, y_anchor, x_val):
     return m * (x_val - x_mid) + y_anchor
[diff_block_end]

Please note that the above snippet only shows the MODIFIED lines from the last change. It shows up to 3 lines of unchanged lines before and after the modified lines. The actual file contents may have many more lines not shown.
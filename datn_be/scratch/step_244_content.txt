Created At: 2026-06-05T13:42:03Z
Completed At: 2026-06-05T13:42:03Z
File Path: `file:///c:/Solar_Inspection_Project/datn_be/scratch/edge_grid_full_image_DJI_0029.py`
Total Lines: 931
Total Bytes: 40708
Showing lines 440 to 480
The following code has been modified to include a line number before every line, in the format: <line_number>: <original_line>. Please note that any changes targeting the original code should remove the line number, colon, and leading space.
440:         b_seg = x0 - a_seg * y0
441:         
442:         denom = 1.0 - m * a_seg
443:         if abs(denom) < 1e-6:
444:             continue
445:         y_inter = (m * (b_seg - x_mid) + y_anchor) / denom
446:         if y0 <= y_inter <= y1:
447:             x_inter = a_seg * y_inter + b_seg
448:             return [int(round(x_inter)), int(round(y_inter))]
449:             
450:     y_fallback = (boundary_pts[0][1] + boundary_pts[-1][1]) / 2.0
451:     x_fallback = get_boundary_x_at_y(boundary_pts, y_fallback)
452:     return [int(round(x_fallback)), int(round(y_fallback))]
453: 
454: def draw_polyline(img, pts, color, thickness=2):
455:     for idx in range(len(pts) - 1):
456:         p1 = (int(round(pts[idx][0])), int(round(pts[idx][1])))
457:         p2 = (int(round(pts[idx+1][0])), int(round(pts[idx+1][1])))
458:         cv2.line(img, p1, p2, color, thickness, lineType=cv2.LINE_AA)
459: 
460: def main():
461:     img_path = extract_input_image()
462:     img = cv2.imread(str(img_path))
463:     if img is None:
464:         raise ValueError(f"Could not read image at {img_path}")
465:         
466:     gray = cv2.cvtColor(img, cv2.COLOR_BGR2GRAY)
467:     clahe = cv2.createCLAHE(clipLimit=2.0, tileGridSize=(8, 8))
468:     gray_clahe = clahe.apply(gray)
469:     blur = cv2.GaussianBlur(gray_clahe, (5, 5), 0)
470:     edges = cv2.Canny(blur, 40, 120, apertureSize=3)
471:     
472:     sobel_x = cv2.Sobel(gray, cv2.CV_32F, 1, 0, ksize=3)
473:     sobel_x_abs = np.abs(sobel_x)
474:     
475:     panels = load_panels_from_logs()
476:     blocks = group_into_blocks(panels)
477:     
478:     img_blocks = img.copy()
479:     img_lines = img.copy()
480:     img_panels = img.copy()
The above content does NOT show the entire file contents. If you need to view any lines of the file which were not shown to complete your task, call this tool again to view those lines.
